<nav class="navbar navbar-static-top navbar-default" role="navigation">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Bookdonation", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to "#{t 'home'}", root_path %></li>
        <li><a href="/about"><%= t('about'); %></a></li>
        <li><a href="/books"><%= t('book'); %></a></li>
        <li><a href="/pricing"><%= t('pricing'); %></a></li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <li>
            <% if user_signed_in? %>
                <%= link_to("#{t 'My donations'}", books_path(donor: current_user.id), 
                  class: "btn btn‐primary btn‐lg") %>
                <%= link_to("#{t 'My received books'}", books_path(donee: current_user.id), 
                  class: "btn btn‐primary btn‐lg") %>
                <%= link_to("#{t 'Logout'}", destroy_user_session_path, :method => :delete, 
                  class: "btn btn‐primary btn‐lg") %>

            <% else %>
              <p>
                   <%= link_to "#{t 'login'}", new_user_session_path, class: "btn btn‐primary btn‐lg" %>
                   <%= link_to "#{t 'signup'}", new_user_registration_path, class: "btn btn‐primary btn‐lg" %>
              </p>
            <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>