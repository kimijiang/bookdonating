<%= form_for(@add_author_to_book) do |f| %>
  <% if @add_author_to_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_author_to_book.errors.count, "error") %> prohibited this add_author_to_book from being saved:</h2>

      <ul>
      <% @add_author_to_book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
