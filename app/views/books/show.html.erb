<%= image_tag @book.image.url %>

<p>
  <strong>Name:</strong>
  <%= @book.name %>
</p>

<p>
  <strong>Isbn:</strong>
  <%= @book.isbn %>
</p>

<%= link_to 'Edit', edit_book_path(@book) if (can? :update, @book)%> |
<%= link_to 'Back', books_path %>
